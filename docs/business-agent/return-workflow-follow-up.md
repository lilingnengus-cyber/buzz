# 退货助手接入：事务基础与剩余工作

## 已验证的 Core 修复

1. 采购退货发运和供应商签收改用供应商范围，避免把 supplier_id 当 customer_id 检查。撤销供应商范围后，新操作和原幂等请求均不可访问。
2. 销售/采购退货草稿对来源单据取得排他锁后再计算剩余可退量。隔离 PostgreSQL 的插入门闩测试让两个请求进入可观察的锁等待；旧实现两张草稿都占用最后一件，修复后仅一张成功，另一张因剩余不足被拒绝。
3. 两类退货确认同时更新持久化版本和更新时间，修复返回 v2、实际仍 v1 的错误。取消、质检和物流状态更新也刷新 updated_at。
4. 创建、确认、取消、质检读取/写入、采购发运/签收补齐来源订单业务单元、订单品牌、来源行冻结品牌与当前商品品牌范围。原有法人、仓库、客户/供应商和操作权限检查继续执行。

## 验证

隔离 B2 全闭环通过，包含现有订单、履约、结算、取消、库存逆转以及新增两类并发退货测试。新采购链为采购订单→收货→退货创建/确认→发运→供应商签收，核对 v1/v2/v3/v4、幂等、撤权、过期版本、库存仍为 1 件和应付仍为 100。新销售链为期初→销售→出库→退货确认→半数接受/半数报废，核对 v1/v2/v3、库存 0.5 件、隔离 0、库存价值 25、应收 100。撤销业务单元或品牌范围的写入与质检读取拒绝通过。

负向证据：旧供应商检查使合法发运返回 NotFoundOrForbidden；旧共享锁使两张并发草稿同时成功；修复权限后测试进一步发现并证明持久化版本缺陷。最终数据库 `return_scopes_final2` 全闭环通过。Core 严格 Clippy、格式和文件大小检查结果见本轮日志。

本批尚未部署，不代表助手已支持退货。线上仍为 83 工具配套版本。

## 接下来必须完成

- 固定、严格输入的销售/采购退货来源与逐行可退量查询；现有 options/list 还需核对业务单元、品牌及分页范围，不能直接作为助手授权入口。
- 退货草稿创建与修改、草稿取消；已确认退货的纠错补偿语义仍需实现，不能伪装成普通出入库逆转。
- 确认前展示数量、成本、隔离库存及应收应付影响；将来源、退货、往来和库存状态绑定服务端意图，事务锁等待后再次比较。
- 销售质检绑定逐行接受/报废数量、日期与成本；采购发运/供应商签收绑定物流证据与日期。
- Gateway 能力、Read API 委托范围、MCP 固定工具、Host 完整签名指令及缺字段补问；不允许模型以普通参数代替用户签名确认。
- 隔离正常/撤权/过期/并发闭环和工具运行时验证，配套发布、实际聊天验收。

后续盘点、主数据、CRM、费用、报表与行动等仍按完整覆盖清单推进，不能以退货基础修复替代整体目标。

## Core 来源读取与草稿入口

新增服务鉴权入口 `GET /v1/agent-return-sources/{sales_return|purchase_return}/{id}` 与 `POST /v1/agent-drafts/returns/{sales_return|purchase_return}`。来源返回当前版本、逐行来源数量、已被草稿/确认退货占用数量、剩余可退量、原始金额/成本、SKU 与范围字段。金额和数量使用十进制字符串；采购来源成本只代表原收货成本，不能当成最终退货确认成本（后者仍依赖执行时移动平均成本）。

草稿入口要求正数 expectedSourceVersion，Core 在来源排他锁取得后比较，随后重新计算可退量。原浏览器输入仍可省略该字段，省略时序列化不会新增字段，保留既有幂等摘要兼容性。创建仅保存 draft，不改变库存或往来，不表示退货确认成功。

新隔离数据库 `agent_return_drafts` 完整 B2 闭环通过：销售/采购来源逐行读取、当前版本、缺版本拒绝、旧版本冲突、未知字段拒绝、品牌/业务单元/仓库撤权后读写拒绝、草稿创建、幂等重放、同键变更原因冲突、创建后可退量减少。此前并发退货及完整销售质检/采购发运签收链继续通过。Core 严格 Clippy、格式与文件大小门禁通过。

这是 Core 入口验证，尚未接入 Gateway/Read API/MCP/Host，尚未部署或进行真实聊天验收。退货修改、确认影响预览和锁内状态绑定、质检/物流签名意图及已确认退货纠错仍未完成。

## Core 退货确认预览与绑定执行

新增两类 `agent-approval-previews/returns` 与 `agent-approvals/returns` 服务入口，复用现有审批策略、票据与完整确认命令结构。预览包含来源、退货版本、日期/原因、逐行库存与隔离影响、退货成本、原始往来与未结余额变化。原始单据字段与完整 guard 一起进入 previewHash；普通浏览器确认仍保留原输入和幂等摘要。

执行在来源/订单、往来及库存锁取得后重查来源与订单版本、往来版本、库存数量/价值/预留/隔离/最后流水和移动平均成本；变化则拒绝，需要重新预览和确认。预览也取得相关锁读取一致的状态，结束后回滚只读事务，不改变业务记录。Core 将上游已验证的确认信息绑定到预览，不代表 Gateway/Host 的实际签名链路已完成。

采购退货清空某 SKU 库存时结转全部剩余库存价值，部分退货后重新计算余额平均成本，防止舍入造成流水成本与库存余额不一致。确认实现拆入独立文件，保持每文件大小限制。

迁移 0041 扩展两类审批单据约束、登记六项 read/create/approve 能力；不自动授予权限或创建策略。完整签名工具与质检/物流处置意图尚未接入，迁移及服务尚未部署。

隔离数据库 `return_confirmation_final3` 的 B2 全闭环通过，包含两类预览→确认→处置和逐行效果回读；旧摘要、夹带字段、撤权、重复确认被拒绝。审批持久化插入门闩在预览核对后暂停请求，期间增加库存预留，再放行执行；正确实现返回 409，负向控制跳过库存检查后错误执行并返回 200，测试失败，已恢复防护。实际期初→采购收货→销售出库→全量采购退货场景证明清空后数量、价值及流水成本均归零。最终相关测试与严格 Clippy 通过。

## Core 质检、发运与签收意图

新增 `sales_return_inspection_intent`、`purchase_return_dispatch_intent`、`purchase_return_acknowledgment_intent`。固定接口提供不持久化的影响预览、30 分钟不可变意图、重读预览及完整确认/拒绝命令。三类命令分别反序列化为既有严格 DTO，拒绝未知字段；外层 sourceDocumentId 与 command 也拒绝未知字段。

质检绑定所有退货行的接收/报废量、冻结成本、数量/价值/预留/隔离与最后流水，锁内重查后执行。发运绑定当前退货版本、日期、承运商和运单号；供应商签收绑定版本、日期和说明，两者只改变物流状态，不改变库存或往来。范围涵盖法人、业务单元、仓库、客户/供应商、冻结和当前品牌；预览返回带范围信息的行。确认仅从已存意图取参数。原浏览器输入和幂等摘要保留。

迁移 0042 新建意图表与不可变触发器，扩展审批/委托类型并登记六个 create/approve 能力，不自动授权或初始化策略。

隔离新库 `return_disposition_final` 全闭环通过：三类准备/确认、幂等准备、同键变参冲突、拒绝不执行、过期意图、未知字段/非法参数、仓库撤权、旧摘要、确认夹带参数、重复确认与数据库禁止修改意图。采购发运/签收还验证既有业务幂等重放。质检审批在预览核对后的真实数据库锁等待期间，新增并过账 0.25 件、成本 12.5 的测试期初库存；执行拒绝旧影响，重新准备后实际库存/成本与新预览一致。故意移除锁内库存检查后出现错误执行（200 而非 409），负向测试失败，恢复检查后全绿。Core 严格 Clippy、格式和文件大小门禁通过。

本批未部署，未发实际聊天消息。下一步集中接入退货来源/详情/列表查询及草稿、确认、三类处置的 Gateway/Read API/MCP/Host；补齐助手修改/取消与已确认退货纠错仍是完整流程的剩余项，不能将本批 Core 验证视为聊天功能上线。

## 退货助手工具链候选版（99 工具，未部署）

新增 6 项读取：销售/采购退货搜索、来源逐行可退量、确认影响预览。搜索按授权范围先过滤再分页，支持精确 ID、字面编号、往来方与状态；Read API 再校验委托范围，包括冻结品牌和当前商品品牌。委托过滤导致空页时仍保留来源 nextOffset，避免误判查询结束。

新增 10 项写入：销售/采购退货草稿和确认，以及销售质检、采购发运、采购签收各自的准备与确认。MCP 输入使用封闭 DTO，数量为十进制字符串，草稿绑定来源版本；5 个审批工具无参数，只消费 Host/Gateway 从当前签名消息验证并绑定的单据类型、ID、版本、摘要与决策。质检/物流准备先做只读预览并检查委托范围，再保存不可变意图。普通“执行”不会变成审批授权。

Gateway 固定能力集 63 项，Host 普通会话申请 40 项（15 项读取），审批能力仅随对应完整签名命令申请。运行时验收通过 buzz-agent 实际启动候选 MCP 和本地模型探针，模型只见 99 项固定工具。未调用真实模型或发送聊天消息；未产生生产业务写入。候选二进制位于 `/tmp/business-read-mcp-return-candidate99`，原 83 工具回退二进制已恢复，已安装客户端和线上服务未变更。

验证：隔离 PostgreSQL 数据库 `return_tools99_final` 的完整 B2 流程通过；查询契约 8、Gateway 8、Read API 26、MCP 13、Host 10 项相关测试通过。新增证据覆盖退货查询权限、分页、错误 trace、严格输入、5 类完整审批命令、品牌变化拒绝、3 类准备的越权请求在保存前被拒绝。Core/Read API/MCP/Gateway 严格 Clippy、格式和文件大小门禁通过。日志：`/tmp/business-return99-{core,tools,clippy,runtime,size}.log`，Host 日志 `/tmp/business-return-all-host-tests.log`。

链接目前明确标注“查看关联履约单据”，指向已存在的出库/收货详情；不能称为退货详情。独立退货详情页、草稿修改/取消、确认后的纠错补偿，以及配套部署和真实聊天验收仍待完成。此候选版不是完整业务覆盖的完成证明。

## 独立退货详情与直接链接（源码完成，未部署）

新增 `/embed/sales-returns/{id}` 和 `/embed/purchase-returns/{id}` 页面，读取对应 `/api/v1/.../{id}` 浏览器会话接口。接口复用退货搜索的全部权限过滤，包括法人、业务单元、往来方、仓库、冻结品牌与当前商品品牌；缺少会话返回 401，撤销范围后返回 404，不通过服务身份绕过浏览器会话。

页面展示对应单号、日期、生命周期与处置状态、原因、备注、金额/成本和逐行商品数量成本，并提供关联出库/收货链接。退货搜索/确认预览、创建草稿、确认以及质检/物流准备和执行结果现在返回退货本身的 `biz://sales-return/{id}` 或 `biz://purchase-return/{id}`；来源查询仍返回原履约单据。Desktop 路由与 MCP 结果 URI 校验同步支持两种链接。

验证：隔离数据库 `return_detail_verified` 完整 B2 闭环通过，使用真实测试浏览器会话验证详情读取与品牌撤权拒绝，并核对确认及处置结果链接指向退货 ID。Playwright 两类详情成功/无权限共 4 项通过（API 响应为测试夹具），桌面链接解析 23 项通过；Web 构建、类型及金额数量展示检查、Desktop 类型检查、相关 Rust 测试和严格 Clippy、格式与文件大小检查通过。日志 `/tmp/business-return-detail-{core,ui,links,tools,contracts,web-build,web-check,desktop-tsc,clippy,size}.log`。

以上替代前节“仅关联履约链接”的候选行为。尚未配套部署或更新已安装客户端；旧 99 工具候选二进制也尚未重建到此详情链接版本。草稿修改/取消、退货纠错补偿及真实聊天验收继续推进。

## 退货草稿修改工具（101 工具候选，未部署）

新增 `update_sales_return_draft` 与 `update_purchase_return_draft`，接入 Gateway、Read API、MCP 与 Host。固定输入为目标退货 ID 和完整替换字段，必须提供退货版本、原履约来源版本、日期、原因及全部目标行；数量使用十进制字符串。原出库/收货来源不可更换，备注省略表示清空。Core 只允许 draft，已确认或已取消的退货不可修改。

Core 新增 `PUT /v1/agent-drafts/returns/{kind}/{id}`。事务按退货→来源顺序锁定，与退货确认一致；来源锁与创建退货共用，取得锁后重新检查版本及其他草稿/确认退货占用数量。自身原占用量不重复扣减，行替换、版本递增、事件/审计与幂等结果同事务提交。幂等摘要包括目标 ID，防止同键同输入错误重放到另一张退货。

新增仅供代理写入检查的来源读取入口，先验证目标退货再读取完整原履约来源。Read API 在转发更新前校验整个来源的委托范围，包括尚未被原退货选中的来源行的冻结/当前品牌，避免通过换行突破权限。迁移 43 仅注册两个 IAM 能力，不自动授权。Gateway 能力集 65，Host 普通会话申请 42，候选工具 101。

隔离数据库 `return_draft_edit_final` 的完整 B2 流程通过：修改、当前版本读取、幂等重放、跨单据同键冲突、退货与来源旧版本冲突、未知字段/重复行/非正数/超量拒绝、品牌撤权、不可换来源、已确认与已取消拒绝，以及取消后释放数量。并发验收先持有来源行锁，确认编辑和创建两个事务均进入实际数据库锁等待后释放，验证仅一方可占用剩余半单位。生产业务记录未改变。

Gateway 8、Read API 27、MCP 13、Host 10 项相关测试通过，严格 Clippy、格式与文件大小检查通过。实际 buzz-agent + 候选 MCP + 本地模型探针确认模型只见 101 项固定工具。候选二进制 `/tmp/business-read-mcp-return-candidate101` 包含退货详情链接和修改能力；已恢复原 83 工具回退二进制，线上与已安装客户端未更新。日志 `/tmp/business-return-edit-{core-final,tools-final,host,clippy-final,runtime,size}.log`。

草稿取消目前仍只有既有 Core/工作台入口；助手的取消预览与签名确认链路尚未接入。后续需完成该链路、已确认退货纠错、配套部署与真实聊天验收，不能以本批修改能力代替完整覆盖。

## 退货草稿取消签名链路（105 工具候选，未部署）

新增销售/采购 `prepare_*_return_cancellation` 和无参数 `approve_*_return_cancellation`，复用 30 分钟不可变退货处置意图、完整签名确认/拒绝、审批票据与审计。准备必须提供当前退货版本和非空取消原因，展示目标单号、各行释放的可退数量、取消后状态以及库存/应收应付变化为零；准备本身不取消。Core 仅允许草稿，执行取得退货锁后重新核对版本；草稿修改、已确认或已取消均会使旧取消指令不可用。

取消原因进入不可变意图、退货事件和审计。既有 Core 取消幂等重放增加目标 ID 一致性检查，保留原摘要兼容性；同单据原请求可重放，同键不可跨单据返回旧结果。迁移 44 扩展退货处置类型并注册 4 个 IAM 能力，无自动授权或审批策略。Gateway 能力集 69，Host 普通会话申请 44，实际 MCP 模型可见 105 个固定工具。

隔离 PostgreSQL `return_cancel_verified` 全部 B2 流程通过：销售/采购准备、理由校验、未知字段拒绝、幂等、不可变/过期、拒绝不执行、撤权拒绝、正常取消、重复/已确认/已取消拒绝、取消原因审计和跨单据幂等冲突。竞态测试在预览校验后的审批插入点持有数据库锁，确认审批实际等待后通过真实草稿修改接口递增版本；释放锁后旧审批返回冲突，重新准备后成功。库存流水数量与往来金额/余额/版本前后相同，后续新草稿可以重新使用释放的数量。

Gateway 8、Read API 28、MCP 13、Host 10 项相关测试通过；两类取消准备均验证委托范围在保存意图前检查，两个确认工具的 schema 无参数。严格 Clippy、格式和文件大小检查通过。实际 buzz-agent + 候选 MCP + 本地模型探针只暴露 105 项固定工具。日志 `/tmp/business-return-cancel-{core-verified,tools-final,host,clippy,runtime,size}.log`。

候选 `/tmp/business-read-mcp-return-candidate105`；已恢复原 83 工具回退二进制，线上和已安装客户端未更新，没有发送真实聊天消息或创建生产业务记录。本节替代前节“助手尚无草稿取消链路”的状态。仍需已确认退货纠错语义与实现、配套部署、真实聊天验收以及完整覆盖清单中的其他业务域。

## 已确认退货冲销预览基础（仅 Core，未部署）

新增严格输入的 `POST /v1/agent-return-reversal-previews/{kind}/{id}`，支持销售/采购已确认退货。锁定退货、原履约/订单、往来余额和相关库存后，核对当前版本、权限、业务日期及原始库存流水，计算逐笔反向流水、数量/隔离量/成本变化和应收应付恢复金额。预览回滚事务，不创建审批、意图或业务记录。

迁移 45 为新库存流水增加不可变入账序号：事务开始时间不能代表等待库存锁后的实际入账顺序。历史流水不回填虚构顺序；新记录必须具有正序号。预览拒绝缺少原始顺序证据、存在后续或夹在退货与报废之间的其他库存变动，以及导致预留量/隔离量/库存价值冲突的冲销。这是保守的原单逆转前提，尚未提供历史或后续业务已发生情况下的调整方案。

隔离数据库 `return_reversal_preview_verified` 完整 B2 流程通过，覆盖销售待质检、采购确认及签收后的预览、重复读取不变、版本/日期/输入/品牌撤权拒绝、夹杂其他库存流水拒绝，以及显式空入账序号被数据库拒绝。预览前后库存流水、审批和意图数量、库存价值及往来余额保持相同。Core 严格 Clippy、格式和文件大小检查通过；日志 `/tmp/business-return-reversal-preview-core-verified.log`、`/tmp/business-return-reversal-preview-clippy.log` 和 `/tmp/business-return-reversal-preview-size.log`。

本批没有实际冲销执行、冲销审批意图或助手工具，候选仍为 105 项，线上仍为 83 项；未部署、未发送真实消息、未写入生产业务记录。下一步需在同一事务内重算并核对签名确认的快照，原子写入反向流水、库存/往来余额、退货状态与审计，再接入完整助手确认链路。

## 已确认退货原子冲销与 Core 审批意图（未部署）

迁移 46 增加退货 reversed 状态、三种反向库存流水类型及销售/采购退货冲销意图类型。预览和执行共用持锁计划：执行事务重算并完整比较获批快照，随后追加与原流水逐笔关联的反向流水，更新库存数量/隔离量/价值/均价及最后流水，恢复应收应付原额和未结余额，保留已结金额，更新退货状态与版本，并写入往来事件、退货事件、审计和幂等结果。原退货行和原库存流水不改写。所有更新同一事务提交，往来版本由既有数据库触发器递增。

新增 `sales_return_reversal_intent`、`purchase_return_reversal_intent`，复用 30 分钟不可变意图、准备/预览、拒绝和确认接口；审批执行调用强制快照校验的服务方法。幂等摘要绑定目标 ID、输入和获批快照，重放前重新检查权限。未新增绕过审批的 HTTP 执行入口。Core 沿用服务边界内的审批上下文；Host/Gateway 的签名消息能力及 MCP 工具尚未接入，不能称为聊天端可用。

隔离数据库 `return_reversal_intents_verified` 完整 B2 流程通过，覆盖销售待质检、销售质检含报废、采购签收后三种冲销。测试验证逐笔反向数量/成本、原流水不变、余额与版本、原因/快照事件、幂等重放、再次冲销与撤权拒绝。持有真实往来行锁并观察执行事务进入等待，再改变往来版本，验证旧快照被拒绝；另在库存和往来更新后的退货事件插入点注入失败，验证全事务回滚且同键可重试。两类意图还验证未知输入、过期、不可变性、拒绝、范围撤销、错误摘要、重复审批及独立退货链接。Core 严格 Clippy、格式、文件大小门禁通过。日志 `/tmp/business-return-reversal-intents-core-verified.log`、`/tmp/business-return-reversal-execution-clippy.log`、`/tmp/business-return-reversal-execution-size.log`。

仍是未部署的源码能力：候选工具数 105、线上 83 未改变，无生产写入或真实消息。下一步接入 Gateway/Read API/MCP/Host，并验证报表与详情对冲销的展示和日期口径。保留此前历史流水顺序缺失、后续库存业务已发生时拒绝自动逆转的限制，其他调整方案仍待实现；完整业务覆盖目标未完成。
