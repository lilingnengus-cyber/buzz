# 企业助手完整业务流程覆盖

目标：企业助手写入覆盖完整业务流程。首批履约上线不能作为目标完成证据。

以 Business Core README 明确的 B1/B2/B3/B4/S1 业务边界及当前业务 API 为实现基线：销售、采购、库存、经营应收应付、真实利润与管理报表；银行连接、财务总账、法定报表当前不是 Core 已实现能力。业务记录中的付款确认不得宣称发起了银行转账。

## 覆盖与剩余工作

| 环节 | 当前证据 | 待完成 |
|---|---|---|
| 基础资料 | 按名称查找已上线 | 客户、供应商、商品/SKU、仓库及相关基础资料的创建、修改、停用，当前版本与范围检查 |
| 售前 CRM | 浏览器创建、修改、跟进已上线 | 助手录入商机、更新阶段和新增跟进，目标查找、字段补问与详情链接 |
| 销售订单 | 助手草稿、签名确认及带原因的全部剩余量取消已部署 | 实际聊天验收；暂停/恢复，部分选定行或数量的取消 |
| 采购订单 | 助手草稿、签名确认及带原因的全部剩余量取消已部署 | 实际聊天验收；采购申请及补货到采购的转换 |
| 库存履约 | 期初、出库、收货草稿及签名确认、三类逆转和来源查询已部署 | 实际聊天验收；盘点录入/差异过账，以及更完整的逐行来源明细查找 |
| 客户收款 | 草稿、确认、核销意图及单据查找已部署；隔离 Core 闭环通过 | 实际聊天端到端验收（核销及收款逆转现已部署） |
| 供应商付款 | 草稿、确认、核销意图及单据查找已部署；隔离 Core 闭环通过 | 实际聊天端到端验收（核销及付款逆转现已部署） |
| 退货 | Core 已有销售/采购退货与处置能力 | 助手创建、修改、确认、逆转，数量/成本/库存/往来影响及处置 |
| 履约细节 | Core 已有交付/到货等操作 | 将现有运输、交付状态等业务写入接入受控工具 |
| 经营费用/利润 | 助手可读利润及证据 | 费用调整草稿、修改、确认、分摊和逆转 |
| 管理报表 | 助手可读报表与快照 | 创建不可变管理报表快照及现有报表写入操作 |
| 异常与行动 | 分析/建议/待办可读，浏览器操作已有 | 明确授权创建待办与审批草稿、事件认领和状态流转；不把建议直接视为完成 |

权限管理与审批策略配置仍通过独立管理员授权路径，不允许业务助手自行提权。所有未覆盖的业务操作仍计为未完成，不因清单完成或某阶段测试通过而宣告整体目标完成。

## 逐项完成证据

每个写入需同时证明：固定工具与严格输入；当前用户能力和数据范围；名称/编号定位及缺字段补问；读取当前版本；影响真实状态的操作有服务端预览和签名确认；参数与确认绑定；既有业务事务执行；幂等、版本冲突、撤权、错误及审计；结果回读和对应详情链接。通过隔离环境实际闭环验证后再部署，真实业务写入测试仍需具体记录授权。

核销、退货等带金额/明细的确认不能仅绑定单据 ID。下一阶段需要服务端保存确定的操作意图，将全部目标、数量、金额和版本纳入摘要，确认工具从绑定意图取值，不能让模型在确认时另传金额或目标。

## 2026-09-19 收付款确认开发证据

新增两组 `get_customer_receipt_approval_preview` / `approve_customer_receipt`、`get_supplier_payment_approval_preview` / `approve_supplier_payment`，当前共 52 个固定工具。确认只改变业务收付款记录至 confirmed，待核销金额等于已核实金额，不改变应收应付核销额，不发起银行交易。

Core 隔离测试覆盖两类记录：精确金额字符串、确认影响、缺审批策略拒绝、撤销往来方范围后读取及执行均拒绝、失效摘要拒绝、成功确认和重复确认拒绝。Gateway/Read API/MCP 单元及 Host 定向测试、严格 Clippy、运行时工具集合探针分别作为分层证据。迁移 0036 不自动授予权限或创建策略；本批未部署，生产仍为上一批 48 工具配套服务。


## 2026-09-19 不可变核销意图开发证据

新增 `prepare_receivable_allocation`、`prepare_payable_allocation` 和对应无参数 `approve_*` 工具，源代码共 56 个工具。准备意图绑定来源版本、所有目标版本和精确金额，30 分钟有效；确认阶段只使用服务端已保存的意图。准备与确认均校验当前权限和完整数据范围，Read API 在持久化意图前预查授权范围。既有浏览器核销 API 及其幂等摘要保持兼容。

隔离 PostgreSQL 闭环覆盖应收和应付两类：幂等准备、相同键更改金额冲突、不可变数据库记录、确认输入夹带金额/目标拒绝、目标行锁等待期间版本变化拒绝且来源余额不变、重新准备后成功核销、重复确认拒绝及目标余额精确减少。另有 Read API 越权目标不能保存意图的集成测试。Gateway、Read API、MCP 单元测试，Host 定向 10 项测试和四服务严格 Clippy 通过；`buzz-agent` 加模拟模型的运行时探针验证 56 工具集合。这不是实际客户端聊天验收。

迁移 0037 仅登记能力与扩展约束，不自动授予权限或创建审批策略。本批尚未部署；线上仍为 48 工具服务，实际客户端 MCP 二进制已恢复到匹配线上版本。下一步需补齐/验证助手如何定位收付款来源和目标的当前版本，再将 0036/0037 配套服务与客户端一起发布；核销逆转及其他未覆盖流程仍然计为未完成。

## 2026-09-19 核销单据定位开发证据

增加 `search_customer_receipts`、`search_supplier_payments`、`search_receivables`、`search_payables`，候选工具集合共 60 项。支持单据 ID、编号文本、往来方、状态及真实 offset 分页；返回当前版本和字符串金额。Core 在分页前过滤用户业务范围，Read API 再过滤当前委托范围，并保留原始页的 nextOffset，防止空页造成错误的唯一匹配判断。应收/应付链接打开对应往来方页面，收付款链接打开具体单据。

隔离 Core 闭环验证四类记录的 ID、版本、余额类型、翻页、组合条件、未知输入拒绝及往来方撤权后不可见；查询适配器验证身份透传和过滤后的分页。合同测试 8 项、Read API 20 项、MCP 12 项、Host 10 项通过；四个相关 crate 严格 Clippy 通过，模拟模型运行时探针确认 60 工具。当前客户端 MCP 已恢复线上兼容版本，实际聊天验收仍未完成。本批无需新数据库迁移，将与 0036/0037 配套发布。


## 2026-09-19 收付款及核销发布

服务端已发布 `07e4a083b`，迁移 0036/0037 成功，四服务健康检查通过。四类单据查询与原有销售订单预览线上只读验证通过，Trace ID `2632f043-d003-4bb4-9831-1735b8a49919`。生产记录保持 5 笔销售订单、0 笔收款/付款、0 个核销意图及 0 次审批投票；SO-202609-000005 仍是 v1、200 元草稿。

macOS 客户端 Host 与 60 工具 MCP 配套文件已安装，签名验证通过，两条企业助手配置改用版本固定的 MCP 路径并更新核销提示。运行中的聊天会话是否加载新配置尚未通过实际聊天验收。完整发布与备份记录见 [收付款发布记录](reports/2026-09-19-settlement-writes.md)。下一步继续核销/收付款逆转和订单取消等剩余写入；完整流程目标仍未完成。

## 2026-09-19 核销与收付款逆转开发证据

新增两类核销历史读取、四类逆转准备和四类零参数签名确认，候选工具集合为 70 项。覆盖客户收款、供应商付款、应收核销、应付核销的逆转；来源版本、核销目标版本、原始核销金额和用户原因纳入不可变 30 分钟意图。存在核销金额时拒绝直接逆转收付款；核销逆转恢复来源待核销与目标未结余额，收付款逆转不发起退款或银行交易。

复用既有版本检查与幂等事务，旧浏览器请求摘要保持兼容。新增原因仅由受控逆转入口传入，随底层业务事务写入审计。核销历史读取返回当前来源与目标版本、核销 ID、金额、已逆转状态，按源分页保留 nextOffset，即使当前页被权限过滤为空也能继续查找。

隔离数据库闭环已证明：通过历史查询定位核销→准备/确认核销逆转→余额恢复→准备/确认收付款逆转→状态 reversed 与待核销归零；两类来源均覆盖。还验证空原因、缺策略、确认夹带参数、旧摘要、准备后目标版本变化、重复确认、不可变意图、幂等准备及同键更改原因冲突；两次实际业务逆转的审计均保留原因。

Read API 21 项、MCP 12 项、Gateway 8 项、查询合同 8 项及 Host 定向 10 项通过；相关 crate 严格 Clippy、文件大小门禁通过；模拟模型运行时验证 70 工具。迁移 0038 仅登记能力与约束，不自动授予权限或初始化策略。本批尚未部署，已安装与线上仍为 60 工具配套版本。下一步发布本批，再继续订单取消、履约逆转、退货及其余流程；整体目标仍未完成。


## 2026-09-19 逆转发布及订单取消候选

逆转版本 `e1df57c1d` 已发布，线上迁移到 0038，四服务健康检查与只读验证通过。Mac 配套 Host/MCP 安装为 70 工具版本。实际客户端检查明确遇到 Mac 锁屏，已请求手动解锁；不能据此声称聊天链路已经验收。详见 [逆转发布记录](reports/2026-09-19-reversal-writes.md)。

新增销售/采购订单剩余量取消准备与签名确认，候选共 74 工具，迁移 0039 不自动授予能力或初始化策略。意图绑定完整当前订单、逐行未履约数量、保留已履约数量、销售库存预留释放量及用户原因。取消完成后部分履约单据标为 completed，不能解释成数量全部履约；既有库存过账及往来记录保留。原因写入订单业务事件与审计。

隔离 Core 闭环覆盖两类订单的草稿取消、部分履约取消、完全履约拒绝，证明取消数量与预览一致、物理库存不变、销售剩余预留释放、幂等准备、同键变更原因冲突、缺策略拒绝、确认夹带参数拒绝、旧摘要及重复确认拒绝、原因审计。Read API 21 项、MCP 12 项、Gateway 8 项、Host 定向 10 项、相关严格 Clippy、文件大小门禁与 74 工具运行时探针通过。取消候选尚未部署；下一步发布取消并继续出入库/期初逆转及其余完整流程。

## 2026-09-19 订单取消发布

取消版本 `f37e91dc9` 已发布，迁移 0039 成功，四服务健康检查通过。线上对现有 v1 草稿只读计算取消影响，验证取消 2 件、保留履约量 0、释放预留量 0；未保存意图、未执行取消。Trace ID `26edcefa-d863-4e4b-98d3-d8af846fcf6b`。订单保持 v1、draft、200 元，生产取消意图和审批投票均为 0。

Mac 配套 Host/MCP 及两条企业助手配置更新至 74 工具版本，应用签名通过；兼容 Host 的 10 项定向测试通过。实际聊天会话是否加载新版仍未验收，最近一次界面检查遇到 Mac 锁屏。本次没有发送测试聊天。详见 [订单取消发布记录](reports/2026-09-19-order-cancellation-writes.md)。

下一步实施出库、收货、期初逆转。已核对现有事务和数据库约束，后续需补齐确认前的库存预留/隔离量阻塞原因、关联状态绑定和锁等待后的再检查，见 [履约逆转核对记录](stock-reversal-follow-up.md)。该核对记录不是实现完成证据；其余完整业务流程仍未完成。

## 2026-09-19 履约逆转事务防护

已修复收货/期初逆转在库存锁等待期间的后续流水检查，并增加最后流水来源检查及预留/隔离量领域拒绝。隔离测试覆盖两种时间戳次序的真实行锁等待；负向控制证明移除锁前置检查会让测试失败。恢复后 B2、B3 闭环、Core 严格 Clippy 和文件大小门禁通过。详见 [履约逆转核对记录](stock-reversal-follow-up.md)。

该修复尚未部署，线上仍为 74 工具版本。履约逆转的不可变意图、原因审计与助手工具仍未完成，完整业务流程目标继续保留。

## 2026-09-19 履约逆转 Core 意图闭环

Core 已实现 shipment / goods_receipt / inventory_opening 三类不可变逆转意图、只读影响预览及绑定确认入口，迁移 0040 只登记能力与约束，不自动授予权限或初始化策略。准备阶段绑定来源版本、完整订单、往来版本、库存数量/金额/预留/隔离/最后流水及原因；执行事务取得相关锁后再次比较订单、往来与库存期望。旧浏览器命令参数和幂等摘要保持兼容。

新隔离 B2 场景覆盖三类正常逆转、影响回读、原因审计、幂等准备、同键变更原因冲突、不可变记录、空原因、过期意图、仓库撤权、缺策略、确认夹带参数、失效摘要、重复确认及锁等待期间库存变更拒绝。负向控制跳过余额绑定后发生错误执行并使测试失败，已恢复校验。有效退货阻止来源逆转；已逆转来源不能再确认退货。测试同时发现并修复采购退货创建/确认/取消错误使用客户范围的问题，验证供应商范围撤销后不能创建。

Core 23 项单元测试、独立新库 B2/B3 闭环、Core/Gateway 严格 Clippy 与文件大小门禁通过。Gateway 新增 build.rs 跟踪迁移目录，解决增量构建漏纳新增 SQLx 迁移的问题。

本批是 Core 层证据，尚未接入 Gateway 委托、Read API、MCP、Host 与来源查询，也未部署或进行真实聊天验收。线上和安装客户端仍为 74 工具。下一步补齐固定查询/准备/确认工具及完整签名链路，再配套发布；全流程目标仍未完成。

## 2026-09-19 履约逆转助手候选

已接入三类来源查询、三类准备及三类零参数确认，候选共 83 工具。Gateway、Read API、MCP 与 Host 均登记固定能力和完整确认命令；普通回合仅获得准备权限，确认仍绑定签名消息。Gateway 请求范围维持数据库 48 项上限，当前普通 Host 委托为 33 项。

来源查询支持 ID、单据编号、往来方、状态、offset 与 limit；Core 在分页前校验法人、业务单元、往来方、仓库及品牌范围，Read API 保留未过滤源页的 nextOffset。隔离查询测试验证三类单据当前版本、组合条件、分页、无权不可见和拒绝未知字段。Read API 测试证明过滤后空页仍能继续翻页，并在越权准备意图持久化前拒绝。

合同 8 项、Gateway 8 项、Read API 23 项、MCP 13 项、Host 定向 10 项、Core 隔离闭环、严格 Clippy 与运行时 83 工具探针通过。MCP 对白名单内业务阻塞返回固定处理建议，核对 Trace ID，拒绝透传任意上游消息。未部署前保留线上 74 工具配套路径，候选二进制为 `/tmp/business-read-mcp-stock-candidate83`。下一步配套发布并记录线上只读验证；实际聊天验收仍待完成。

## 2026-09-19 履约逆转发布

版本 `734400865` 已发布，迁移 0040、四服务健康检查、三类库存查询及不存在意图拒绝均通过。线上只读 Trace ID `e2bfd71c-061a-49c2-8a09-abc63afe333d`。现有订单仍为 v1、draft、200 元，库存逆转意图与审批投票均为 0。Mac 配套 Host/MCP 和两条配置已安装为 83 工具版本，文件与签名验证通过。

真实聊天验收仍未完成；本次未发送测试消息或执行生产业务逆转。详见 [履约逆转发布记录](reports/2026-09-19-stock-reversal-writes.md)。下一步继续退货、盘点及其他剩余写入，完整流程目标保持未完成。

## 2026-09-19 退货事务基础验证

已修复采购退货物流权限误用客户范围、并发草稿重复占用可退量及确认返回版本与数据库版本不一致。补齐退货写入的业务单元与品牌范围。隔离闭环证明销售退货质检和采购退货发运/签收可连续执行，重复请求不重复变更库存或往来；真实锁等待负向控制复现旧共享锁的超退问题。

本批尚未部署，助手退货工具、签名预览与确认仍待完成；线上仍为 83 工具。具体证据及剩余工作见 [退货接入记录](return-workflow-follow-up.md)。完整流程目标仍未完成。

## 2026-09-19 退货来源与草稿 Core 入口

两类退货已新增有范围检查的逐行来源读取、要求当前来源版本的草稿创建服务入口。来源锁内核对版本和可退量，省略新增版本字段的既有浏览器输入保持幂等摘要兼容。隔离 B2 闭环验证正常草稿、缺版本/旧版本、越权、未知字段、幂等与占用数量回读通过。

尚未接入助手工具与签名确认，也未部署；线上工具集合仍为 83。下一步继续退货确认/处置的不可变意图和配套工具，完整业务流程仍未完成。

## 2026-09-19 退货确认 Core 闭环

销售/采购退货确认已实现完整影响预览、previewHash 绑定及事务锁内关联状态重查。新增迁移 0041；不自动授予权限或创建策略。隔离闭环通过两类确认与处置、真实并发变更拒绝和全量退货舍入对账；移除库存检查的负向控制产生错误执行并使测试失败，已恢复。

本批仍为 Core 证据；Gateway、Read API、MCP、Host、退货修改/取消和质检/物流完整意图及真实聊天仍有工作，尚未部署，线上维持 83 工具。完整目标未完成。

## 2026-09-19 退货处置 Core 闭环

销售质检、采购发运及供应商签收新增 30 分钟不可变意图与确认/拒绝入口，迁移 0042。隔离闭环覆盖正常、幂等、撤权、过期、参数绑定、拒绝不执行及质检确认期间真实库存过账导致旧预览失效；移除库存防护的负向控制错误执行并使测试失败，已恢复。相关严格 Clippy、格式及文件大小门禁通过。

尚未接入完整助手工具或部署，线上仍为 83 工具。下一步集中接入退货查询、创建、确认、质检和物流工具，并继续补齐修改/取消、纠错及全流程其他剩余写入。

### 2026-09-19：退货工具链候选版

已将销售/采购退货来源、搜索、确认预览、草稿创建、确认、销售质检及采购发运/签收接入 Gateway → Read API → MCP → Host；新增 6 读、10 写，候选 99 工具。隔离数据库完整 B2 流程、越权与严格输入测试、实际 buzz-agent 工具注册探针通过。详见 [退货后续工作](return-workflow-follow-up.md)。

尚未部署，线上/已安装客户端仍为 83 工具。独立退货详情、草稿修改/取消、已确认退货纠错、配套权限策略与发布、真实聊天验收仍未完成；后续盘点、主数据、CRM、费用、报表和行动范围保持不变。

### 2026-09-19：退货独立详情与直接链接

已补齐销售/采购退货独立详情页和浏览器会话详情接口，并将助手结果与 Desktop 路由连到退货本身。隔离数据库真实会话/撤权检查、4 项页面功能测试、23 项桌面链接测试通过。此项源码完成，尚未部署；草稿修改/取消、已确认退货纠错及整体后续范围未完成。

### 2026-09-19：销售/采购退货草稿修改

新增两个固定修改工具，候选共 101 项；绑定退货/来源版本，只替换原履约来源内的草稿行及描述字段。源行锁下重新计算可退余量，验证编辑与创建并发仅一方可占用剩余量；幂等摘要绑定目标 ID。隔离 B2 全流程、范围/版本/幂等测试及实际 agent 工具探针通过。迁移 43 未自动授予权限，尚未部署。助手的草稿取消签名链路及已确认退货纠错仍待完成。

### 2026-09-19：退货草稿取消签名链路

新增两类取消准备及无参数确认，候选 105 工具。不可变意图绑定版本/原因/释放数量，执行时在退货锁内核验；拒绝、过期、越权、预览后真实修改竞态及重复执行均有隔离验证。取消原因留痕，跨单据幂等重放被拒绝。迁移 44 未自动授权，尚未部署或真实聊天验收。已确认退货纠错及其他业务域仍未完成。

### 2026-09-19：已确认退货冲销只读预览

Core 新增销售/采购退货冲销预览，在相关行锁下核算原始流水、库存及应收应付影响。迁移 45 为新流水记录入账序号，拒绝缺少历史顺序证据或存在中间/后续库存变动的自动逆转。隔离 B2 全流程、无写入和权限/输入/顺序校验通过。此项尚无实际冲销执行、审批或新增助手工具，未部署；候选 105、线上 83 的状态不变，完整目标仍未完成。

### 2026-09-19：已确认退货 Core 原子冲销及审批

冲销预览与执行共用事务内计划，获批快照不符即拒绝；追加反向流水并原子更新库存、应收应付、退货状态、审计和幂等记录。两类不可变审批意图已接入 Core，隔离验证覆盖三种退货阶段、真实锁等待后的版本变化拒绝、写入后故障回滚及审批/幂等/撤权。不改写原流水。迁移 46 未部署；Gateway/Read API/MCP/Host 和报表/详情完整验收、历史或后续业务纠错方案仍待完成，工具数和线上版本不变。

### 2026-09-19：退货冲销助手工具候选 109

两类冲销准备及无参数签名确认已接入 Gateway/Read API/MCP/Host，严格输入、委托检查、签名类型绑定和实际 agent 工具注册验证通过。迁移 47 不自动授权，尚未部署。发现发布阻塞：利润投影未消费冲销，延迟确认投影还会因当前状态 reversed 失败；需先补齐相应报表闭环和验证。完整业务范围与其他未完成项保持不变。
