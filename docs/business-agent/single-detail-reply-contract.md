# BizOS 单条详情回复契约

单条详情回复按固定顺序呈现实际返回且适用的信息：业务编号、状态、金额／数量、业务日期、关键归属、详情链接；随后再显示查询记录和最终追踪号。

成功查询的固定末尾顺序为：数据时点、查询记录、追踪号。先呈现业务字段和详情链接；只保留已验证且非空的字段。字段未返回、不适用、`{}`、`[]`、`null`、`undefined` 或“工具未说明”时跳过，不补造，也不为了凑格式重复信息。批量列表继续遵循其独立的逐项展示规则。

自动契约测试位于 `crates/buzz-acp/src/business_agent/tests.rs`。

## 2026-09-26 客户端验收

19:38（UTC+8）在与 BizOS 的私聊发送只读查询“查询销售订单 SO-202609-000005”。回复依次展示可点击的业务编号、草稿状态、CNY 200.00 金额、2026-09-19 订单日期、库存与履约状态、详情链接、查询记录和最终追踪号；未展示内部 UUID，也未补造未返回字段。查询记录及追踪号为 `1999b5c3-a844-4c2f-8684-57f5a544a171`。本次未创建或修改业务数据。
