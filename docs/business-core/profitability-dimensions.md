# Profitability dimensions

The same scoped profit facts can be grouped by group, legal entity, customer,
SKU, product category, brand, salesperson, business unit, department, warehouse
or sales order, with an optional distinct second dimension. The B1 legal-entity,
customer, brand and business-unit scopes are enforced in SQL. One request is one
currency and management period; currencies are never silently combined.
