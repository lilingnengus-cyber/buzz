import { BusinessDock } from "@/features/business-dock";

/** Product-specific dock kept behind one stable Buzz shell integration point. */
export function AppExtensionDock() {
  return <BusinessDock />;
}
