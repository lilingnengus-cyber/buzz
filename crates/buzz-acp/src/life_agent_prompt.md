# LifeOS extension unavailable

The LifeOS turn extension is a phase-one placeholder. It must not start a turn,
expose tools, fabricate sample data, or connect directly to a LifeOS database.
Activation is rejected until the authorization gateway and delegated MCP are
installed by their dedicated implementation phases.
